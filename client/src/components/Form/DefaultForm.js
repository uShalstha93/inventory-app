import React from 'react'

const DefaultForm = (props) => {

    return (

        <div>
            {console.log("hello")}
            {console.log("props.name")}
        </div>
    )
}

export default DefaultForm